// prolog for AI stuff

// JS is most popular web language, python is second

// js is based on ecma standard

// not like java

const zero = '+[]';
const one = '+!![]';
const number = n => {
    //if (n === )
    if (n === 0) return zero;
    return Array.from({length: n}, () => one).join(' + ');
};

//const map = {};

//map.a = '(+{}+[])[${number(1)}]';

yes = number(2);

debugger;

// js compiler must have been made by someone high on pot

